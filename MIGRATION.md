# 移行手順（旧スキーマ → 新スキーマ）

既に旧バージョンを運用しているプロジェクト向けの手順です。**新規に導入する場合は不要です**（README のセットアップに従ってください）。

## 何が変わるか

| | 旧 | 新 |
|---|---|---|
| メンバー | `members/{メールアドレス}` | `groups/{gid}/members/{uid}` |
| シフト | `shiftEntries/{メール}_{日付}` | `groups/{gid}/shifts/{...}` |
| 招待コード | `config/settings` | `groups/{gid}/settings/general` |
| 権限 | 全員が対等 | 管理者 / リーダー / メンバー |
| 1日のシフト | 1件まで | 複数（時間帯で分割可） |

## 進め方の全体像

1. エミュレータで**練習**する（本番に触らない）
2. 本番でドライラン（読むだけ・書かない）
3. 本番で実行
4. 件数を突き合わせる
5. アプリで目視確認
6. 数日おいてから旧コレクションを手動削除

**旧コレクションはスクリプトが削除しません。** 失敗しても元に戻せます。

---

## 事前に決めておくこと

- **誰を管理者にするか**（メールアドレス1つ）。他の全員はメンバーになります。あとから管理者が変更できます
- **切り替える時間帯**。移行中はアプリを使わないようメンバーに周知してください

---

## 1. エミュレータで練習する（推奨）

本番の鍵を使わずに、同じスクリプトを一通り試せます。Java が必要です。

```bash
npm install
npx firebase emulators:start --only firestore,auth --project demo-practice
```

別のターミナルで、旧スキーマのテストデータを入れてから実行します。

```bash
export FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
export FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

node scripts/migrate.mjs --project demo-practice --admin you@example.com --name "練習"
node scripts/migrate.mjs --project demo-practice --admin you@example.com --name "練習" --apply
```

出力の読み方をここで掴んでおくと、本番で迷いません。

---

## 2. サービスアカウント鍵を取得する

Firebaseコンソール → プロジェクトの設定 → **サービスアカウント** → 「新しい秘密鍵の生成」

> **この鍵はプロジェクト全体を操作できます。**
> - リポジトリに置かない（`.gitignore` 済みですが、念のため別の場所に保存してください）
> - 他人に渡さない、チャットに貼らない
> - **移行が終わったら削除する**（コンソールから無効化もできます）

---

## 3. 本番でドライラン

**書き込みは一切行いません。** 何が起きるかだけ表示します。

```bash
node scripts/migrate.mjs \
  --key /path/to/serviceAccount.json \
  --admin you@example.com \
  --name "チーム名"
```

出力を確認してください。

- メンバーの一覧と、それぞれの `メールアドレス → uid` の対応
- 誰が `admin` になるか
- シフトの件数（確定 / 希望の内訳）
- **「注意」欄**に何か出ていないか

### 「Authユーザーが見つかりません」と出たら

Firestore の `members` にいるのに、Authentication にアカウントが無い人です。
そのメンバーとシフトは移行されません。心当たりがなければ、**進めずに調べてください**。

---

## 4. 本番で実行

メンバーに「この時間は使わないで」と伝えてから実行します。

```bash
node scripts/migrate.mjs \
  --key /path/to/serviceAccount.json \
  --admin you@example.com \
  --name "チーム名" \
  --apply
```

- 書き込み前に `backup-<プロジェクト>-<日時>.json` が作られます。**消さずに保管してください**
- 最後に表示される **グループID** を控えてください

途中で失敗しても旧データは無傷です。原因を直して再実行できます（同じIDで上書きされるため、シフトが二重に増えることはありません）。

---

## 5. 件数を突き合わせる

```bash
node scripts/migrate.mjs --key /path/to/serviceAccount.json --verify --group-id <グループID>
```

「一致」と出れば移行できています。

---

## 6. アプリで確認する

新しいコードをデプロイしてから、管理者のアカウントでログインし、次を見てください。

- [ ] メンバーが全員表示されている
- [ ] 過去の希望・確定シフトが正しい日付に出ている
- [ ] 自分が管理者になっている（ヘッダーに「設定」「メンバー」が出る）
- [ ] 一般メンバーでログインすると「確定選択」が**出ない**
- [ ] ヘッダーの「設定」で招待コードが引き継がれている

### 移行後にやること

- **他のメンバーのロールを決める**（「メンバー」画面から、必要な人をリーダーに）
- 招待リンクを配り直す（参加方法が招待コードのみ → 招待リンク＋コードに変わったため）

---

## 7. 旧コレクションを片付ける

**すぐには消さないでください。** 数日〜1週間운用して問題が無いことを確かめてから、Firebaseコンソールで次を削除します。

- `members`
- `shiftEntries`
- `config`

削除後は `backup-*.json` だけが復元手段になります。

---

## 困ったとき

| 症状 | 原因と対処 |
|---|---|
| 「所属しているグループがありません」と出る | `users/{uid}.groupIds` が書けていない。スクリプトを再実行してください |
| ログインできるが自分が管理者でない | `--admin` のメールアドレスが `members` の値と一致していなかった。「メンバー」画面（別の管理者）から変更するか、再実行してください |
| 特定の人だけカレンダーに出ない | ドライランの「注意」欄にその人が出ていたはずです。Authにアカウントがあるか確認してください |
| 全部消えたように見える | 旧コレクションは残っています。新しいコードが旧データを読みに行っていないか（デプロイの前後関係）を確認してください |
