# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Firebase のサービスアカウント鍵。1つでも漏れるとプロジェクト全体を掌握されるため、
# 名前の揺れも含めて広めに除外する。
serviceAccount*.json
*-firebase-adminsdk-*.json
service-account*.json

# 移行スクリプトが出力する旧データのバックアップ（メールアドレスを含む）
backup-*.json

# Cloudflare Worker (カレンダー購読) のローカル状態・秘密情報
.wrangler
worker/.dev.vars
worker/.dev.vars.*
!worker/.dev.vars.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
